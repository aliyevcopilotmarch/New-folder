<Window x:Class="IISHardeningTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TayqaSale IIS Hardening Tool — Avrora Security"
        Height="800" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Window.Resources>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="🛡️ TayqaSale IIS Hardening Tool" 
                       Foreground="#89B4FA" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Automated IIS Security Misconfiguration Remediation — CIS Benchmark Compliant" 
                       Foreground="#6C7086" FontSize="12" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="BtnScanAll" Content="🔍 Scan All" 
                    Background="#89B4FA" Click="BtnScanAll_Click"
                    Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
            <Button x:Name="BtnScanSelected" Content="🔎 Scan Selected" 
                    Background="#74C7EC" Click="BtnScanSelected_Click"
                    Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
            <Button x:Name="BtnFixSelected" Content="🔧 Fix Selected" 
                    Background="#A6E3A1" Click="BtnFixSelected_Click"
                    Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
            <Button x:Name="BtnFixAll" Content="⚡ Fix All Non-Compliant" 
                    Background="#F9E2AF" Click="BtnFixAll_Click"
                    Style="{StaticResource ActionButton}" Foreground="#1E1E2E" Margin="0,0,10,0"/>
            <Button x:Name="BtnSelectAll" Content="☑ Select All" 
                    Background="#585B70" Click="BtnSelectAll_Click"
                    Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
            <Button x:Name="BtnExportReport" Content="📄 Export Report" 
                    Background="#CBA6F7" Click="BtnExportReport_Click"
                    Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
            <Button x:Name="BtnClearLogs" Content="🗑 Clear Logs" 
                    Background="#F38BA8" Click="BtnClearLogs_Click"
                    Style="{StaticResource ActionButton}"/>
        </StackPanel>

        <!-- Items DataGrid -->
        <DataGrid Grid.Row="2" x:Name="DgItems" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Background="#313244" 
                  Foreground="#CDD6F4"
                  BorderBrush="#45475A"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#45475A"
                  RowBackground="#313244"
                  AlternatingRowBackground="#3B3D50"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  FontSize="13">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderBrush" Value="#585B70"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Header="✓" Width="40"/>
                <DataGridTextColumn Binding="{Binding Id}" Header="#" Width="40"/>
                <DataGridTextColumn Binding="{Binding Title}" Header="Misconfiguration" Width="220"/>
                <DataGridTextColumn Binding="{Binding Category}" Header="Category" Width="150"/>
                <DataGridTextColumn Binding="{Binding RiskLevel}" Header="Risk" Width="70"/>
                <DataGridTextColumn Binding="{Binding Status}" Header="Status" Width="110"/>
                <DataGridTextColumn Binding="{Binding StatusMessage}" Header="Details" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Separator -->
        <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch" Background="#45475A" Margin="0,5"/>

        <!-- Log Panel -->
        <DockPanel Grid.Row="4">
            <TextBlock DockPanel.Dock="Top" Text="📋 Operation Log" 
                       Foreground="#A6ADC8" FontSize="12" FontWeight="SemiBold" Margin="0,5,0,5"/>
            <TextBox x:Name="TxtLog" 
                     Background="#181825" Foreground="#A6ADC8" 
                     BorderBrush="#45475A" 
                     IsReadOnly="True" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"
                     FontFamily="Cascadia Code, Consolas, Courier New"
                     FontSize="12"
                     Padding="10"/>
        </DockPanel>
    </Grid>
</Window>
